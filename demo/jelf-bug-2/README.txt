This project is the same as tests/simpleRule.

There is one model, Individual, that has one attribute, that should never be able to be seen by anybody. The bug is that one can create an account, and then see any of the SSNs